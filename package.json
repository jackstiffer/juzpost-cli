{
  "name": "juzpost-cli",
  "version": "0.0.1",
  "description": "CLI for JuzPost — login + smart-schedule clips from the terminal",
  "type": "module",
  "bin": { "juzpost": "dist/index.js" },
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
